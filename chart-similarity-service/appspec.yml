version: 0.0
os: linux

files:
  - source: /
    destination: /home/ec2-user/sol-assistant-backend/

permissions:
  - object: /
    pattern: "**"
    owner: ec2-user
    group: ec2-user
    mode: 755

hooks:
  AfterInstall:
    - location: deploy/run_new.sh
      timeout: 180
      runas: ec2-user
